@page "/chat"
@using AzureAIAvatarBlazor.Models
@using AzureAIAvatarBlazor.Services
@using System.Text.Json
@using Microsoft.JSInterop
@inject IAzureOpenAIService OpenAIService
@inject IConfigurationService ConfigService
@inject IJSRuntime JSRuntime
@implements IAsyncDisposable
@rendermode InteractiveServer

<PageTitle>Chat - Azure AI Avatar</PageTitle>

<div class="container-fluid mt-3">
    <div class="d-flex flex-wrap gap-2 mb-3">
        <button class="btn btn-primary" @onclick="StartAvatarSession" disabled="@(isSessionActive || isSessionStarting)">
            <i class="bi bi-play-circle me-1"></i>Open Avatar Session
        </button>
        <button class="btn btn-secondary" @onclick="ToggleMicrophone" disabled="@(!isSessionActive)">
            <i class="bi @(isMicrophoneActive ? "bi-mic-mute" : "bi-mic") me-1"></i>@(isMicrophoneActive ? "Stop Microphone" : "Start Microphone")
        </button>
        <button class="btn btn-outline-secondary" @onclick="StopSpeaking" disabled="@(!isSpeaking)">
            <i class="bi bi-stop-fill me-1"></i>Stop Speaking
        </button>
        <button class="btn btn-outline-danger" @onclick="ClearChatHistory">
            <i class="bi bi-trash me-1"></i>Clear Chat History
        </button>
        <button class="btn btn-outline-primary" @onclick="StopSession" disabled="@(!isSessionActive)">
            <i class="bi bi-x-circle me-1"></i>Close Avatar Session
        </button>
    </div>

    <div class="row g-3">
        <!-- Left: Avatar Video -->
        <div class="col-12 col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body p-0">
                    <div id="videoContainer" class="video-container" style="min-height: 400px; background: #000;">
                        <div id="remoteVideo"></div>
                        @if (config?.Avatar?.EnableSubtitles == true)
                        {
                            <div id="subtitles" class="subtitles"></div>
                        }
                    </div>
                </div>
            </div>
        </div>

        <!-- Right: Conversation -->
        <div class="col-12 col-lg-4">
            <div class="card shadow-sm h-100">
                <div class="card-header">
                    <h5 class="mb-0"><i class="bi bi-chat-dots me-2"></i>Conversation</h5>
                </div>
                <div class="card-body d-flex flex-column" style="height: 400px;">
                    <div class="chat-history flex-grow-1 overflow-auto mb-3" style="max-height: 300px;">
                        @foreach (var message in chatMessages)
                        {
                            <div class="mb-2">
                                <strong>@message.Role:</strong>
                                <div>@message.Content</div>
                            </div>
                        }
                        @if (isTyping)
                        {
                            <div class="mb-2">
                                <strong>Assistant:</strong>
                                <div class="typing-indicator">
                                    <span></span><span></span><span></span>
                                </div>
                            </div>
                        }
                    </div>

                    <div class="mt-auto">
                        <div class="form-check mb-2">
                            <input type="checkbox" class="form-check-input" id="showTypeMessage" 
                                   @bind="showTypeMessage" disabled="@(!isSessionActive)">
                            <label class="form-check-label" for="showTypeMessage">Type Message</label>
                        </div>

                        @if (showTypeMessage)
                        {
                            <div class="input-group">
                                <input type="text" class="form-control" placeholder="Type a message..." 
                                       @bind="userMessage" @onkeydown="HandleKeyPress" disabled="@(!isSessionActive)">
                                <button class="btn btn-primary" @onclick="SendMessage" disabled="@(!isSessionActive || string.IsNullOrWhiteSpace(userMessage))">
                                    <i class="bi bi-send"></i>
                                </button>
                            </div>
                        }

                        <div class="d-flex flex-wrap gap-2 mt-2">
                            <button type="button" class="btn btn-outline-secondary btn-sm" @onclick="SendHelloAsync" disabled="@(!isSessionActive)">
                                Hello
                            </button>
                            <button type="button" class="btn btn-outline-secondary btn-sm" @onclick="SendDotNetConfAsync" disabled="@(!isSessionActive)">
                                What can you tell me about .NET Conf 2025
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    private AvatarConfiguration? config;
    private List<ChatMessage> chatMessages = new();
    private string userMessage = string.Empty;
    private bool showTypeMessage = false;
    private bool isSessionActive = false;
    private bool isSessionStarting = false;
    private bool isSpeaking = false;
    private bool isTyping = false;
    private bool isMicrophoneActive = false;
    private DotNetObjectReference<Chat>? dotNetReference;

    protected override async Task OnInitializedAsync()
    {
        config = ConfigService.GetConfiguration();
        
        // Add system message
        if (!string.IsNullOrEmpty(config.AzureOpenAI.SystemPrompt))
        {
            chatMessages.Add(new ChatMessage 
            { 
                Role = "system", 
                Content = config.AzureOpenAI.SystemPrompt 
            });
        }
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            dotNetReference = DotNetObjectReference.Create(this);
            await JSRuntime.InvokeVoidAsync("initializeAvatarSDK", dotNetReference);
        }
    }

    private async Task StartAvatarSession()
    {
        try
        {
            Console.WriteLine("Starting avatar session...");
            isSessionStarting = true;
            StateHasChanged();
            
            // Serialize config with camelCase for JavaScript
            var options = new JsonSerializerOptions
            {
                PropertyNamingPolicy = JsonNamingPolicy.CamelCase,
                WriteIndented = false
            };
            var configJson = JsonSerializer.Serialize(config, options);
            
            Console.WriteLine($"Config JSON: {configJson}");
            await JSRuntime.InvokeVoidAsync("startAvatarSessionFromJson", configJson);
            isSessionActive = true;
            isSessionStarting = false;
            Console.WriteLine("Avatar session started successfully");
        }
        catch (Exception ex)
        {
            var errorMessage = ex.Message ?? string.Empty;
            var stackTrace = ex.StackTrace ?? "<no stack trace>";
            var isCancellation = errorMessage.Contains("Avatar start cancelled", System.StringComparison.OrdinalIgnoreCase);

            if (isCancellation)
            {
                Console.WriteLine("Avatar session start was cancelled by the user.");
            }
            else
            {
                Console.WriteLine($"Error starting avatar session: {errorMessage}");
                Console.WriteLine($"Stack trace: {stackTrace}");
            }
            isSessionActive = false;
            isSessionStarting = false;
            StateHasChanged();
            
            if (!isCancellation)
            {
                // Show error to user
                chatMessages.Add(new ChatMessage
                {
                    Role = "system",
                    Content = $"Error starting avatar: {errorMessage}"
                });
                StateHasChanged();
            }
        }
    }

    private async Task StopSession()
    {
        try
        {
            await JSRuntime.InvokeVoidAsync("stopAvatarSession");
            isSessionActive = false;
            isMicrophoneActive = false;
            isSessionStarting = false;
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error stopping avatar session: {ex.Message}");
        }
    }

    private async Task ToggleMicrophone()
    {
        try
        {
            if (!isMicrophoneActive)
            {
                await JSRuntime.InvokeVoidAsync("startMicrophone");
            }
            else
            {
                await JSRuntime.InvokeVoidAsync("stopMicrophone", false);
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error starting microphone: {ex.Message}");
        }
    }

    private async Task StopSpeaking()
    {
        try
        {
            await JSRuntime.InvokeVoidAsync("stopSpeaking");
            isSpeaking = false;
            StateHasChanged();
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error stopping speaking: {ex.Message}");
        }
    }

    private void ClearChatHistory()
    {
        // Keep system message, clear the rest
        var systemMessage = chatMessages.FirstOrDefault(m => m.Role == "system");
        chatMessages.Clear();
        if (systemMessage != null)
        {
            chatMessages.Add(systemMessage);
        }
        StateHasChanged();
    }

    private async Task HandleKeyPress(KeyboardEventArgs e)
    {
        if (e.Key == "Enter" && !string.IsNullOrWhiteSpace(userMessage))
        {
            await SendMessage();
        }
    }

    private Task SendMessage() => ProcessOutgoingMessageAsync(userMessage, clearInput: true);

    private Task SendPredefinedMessage(string presetMessage) => ProcessOutgoingMessageAsync(presetMessage, clearInput: true);

    private Task SendHelloAsync() => SendPredefinedMessage("Hello");

    private Task SendDotNetConfAsync() => SendPredefinedMessage("What can you tell me about .NET Conf 2025");

    private async Task ProcessOutgoingMessageAsync(string? content, bool clearInput)
    {
        if (string.IsNullOrWhiteSpace(content))
        {
            if (clearInput && !string.IsNullOrEmpty(userMessage))
            {
                userMessage = string.Empty;
                StateHasChanged();
            }
            return;
        }

        var message = content.Trim();

        if (clearInput)
        {
            userMessage = string.Empty;
            StateHasChanged();
        }

        Console.WriteLine($"Sending message: {message}");

        // Add user message to history
        chatMessages.Add(new ChatMessage { Role = "user", Content = message });
        isTyping = true;
        StateHasChanged();

        try
        {
            Console.WriteLine("Getting chat completion from OpenAI...");

            // Get messages for API (including system message)
            var messagesForAPI = chatMessages.Select(m => new ChatMessage
            {
                Role = m.Role,
                Content = m.Content
            }).ToList();

            var assistantResponse = new System.Text.StringBuilder();

            await foreach (var chunk in OpenAIService.GetChatCompletionStreamAsync(messagesForAPI))
            {
                assistantResponse.Append(chunk);
            }

            var fullResponse = assistantResponse.ToString();
            Console.WriteLine($"Received response: {fullResponse.Substring(0, Math.Min(100, fullResponse.Length))}...");

            chatMessages.Add(new ChatMessage { Role = "assistant", Content = fullResponse });
            StateHasChanged();

            // Speak the response using avatar
            if (isSessionActive)
            {
                Console.WriteLine("Speaking text via avatar...");
                isSpeaking = true;
                StateHasChanged();

                try
                {
                    await JSRuntime.InvokeVoidAsync("speakText", fullResponse);
                    Console.WriteLine("✅ Avatar speech completed successfully");
                }
                catch (Exception speakEx)
                {
                    Console.WriteLine($"⚠️ Speech error: {speakEx.Message}");
                    // Don't throw - allow conversation to continue
                }
                finally
                {
                    isSpeaking = false;
                }
            }
            else
            {
                Console.WriteLine("Avatar session not active, skipping speech");
            }
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error in chat: {ex.Message}");
            Console.WriteLine($"Stack trace: {ex.StackTrace}");
            chatMessages.Add(new ChatMessage
            {
                Role = "assistant",
                Content = $"Error: {ex.Message}. Please check the browser console for more details."
            });
        }
        finally
        {
            isTyping = false;
            isSpeaking = false;
            StateHasChanged();
        }
    }

    [JSInvokable("OnSpeechRecognized")]
    public async Task OnSpeechRecognizedAsync(string text)
    {
        if (string.IsNullOrWhiteSpace(text))
            return;

        Console.WriteLine($"Speech recognized: {text}");

        await InvokeAsync(() =>
        {
            showTypeMessage = true;
            userMessage = text;
            StateHasChanged();
        });

        await InvokeAsync(async () => await SendMessage());
    }

    [JSInvokable("OnMicrophoneStateChanged")]
    public async Task OnMicrophoneStateChangedAsync(bool active)
    {
        await InvokeAsync(() =>
        {
            isMicrophoneActive = active;
            StateHasChanged();
        });
    }

    public async ValueTask DisposeAsync()
    {
        if (dotNetReference != null)
        {
            dotNetReference.Dispose();
            dotNetReference = null;
        }

        await Task.CompletedTask;
    }
}
